services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATA_DIR=/data
    volumes:
      - shopping-data:/data
    restart: unless-stopped

volumes:
  shopping-data:
